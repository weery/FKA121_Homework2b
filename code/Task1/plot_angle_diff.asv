clear all, clc, close all

angle_diff = importdata('angle_diff.dat');


[h,b]=histcounts(angle_diff);

xlabel('Och jag med','interpreter','latex')
ylabel('Jag är fel','interpreter','latex')
title('En plott som \"är fel och ska rättas till','interpreter','latex')